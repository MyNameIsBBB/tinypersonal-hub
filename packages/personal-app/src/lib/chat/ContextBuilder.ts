const offsetHours = 7;
const pad = (value: number) => String(value).padStart(2, "0");
export function formatBangkokDateTime(date: Date) { const local = new Date(date.getTime() + offsetHours * 3_600_000); return `${local.getUTCFullYear()}-${pad(local.getUTCMonth() + 1)}-${pad(local.getUTCDate())} ${pad(local.getUTCHours())}:${pad(local.getUTCMinutes())}`; }
export function parseBangkokDateTimeInput(raw: string) { const value = raw.trim(); const match = value.match(/^(\d{4})-(\d{2})-(\d{2})[T\s](\d{2}):(\d{2})(?::(\d{2}))?/); const parsed = match ? new Date(Date.UTC(+match[1], +match[2] - 1, +match[3], +match[4] - offsetHours, +match[5], +(match[6] ?? 0))) : new Date(value); if (Number.isNaN(parsed.getTime())) throw new Error("startsAt must be a valid datetime string"); return parsed; }
export function bangkokNowContext(now: Date) { const local = new Intl.DateTimeFormat("en-CA", { timeZone: "Asia/Bangkok", year: "numeric", month: "2-digit", day: "2-digit", hour: "2-digit", minute: "2-digit", second: "2-digit", hour12: false }).format(now).replace(",", ""); return `Current local datetime is ${local} in Asia/Bangkok (UTC+07:00). Interpret relative dates such as today/tomorrow using Asia/Bangkok.`; }
export function buildScheduleContext(items: Array<{ type: string; title: string; status: string; startTime: Date | null; endTime: Date | null }>) { return items.length ? `Relevant schedule items (next 14 days, maximum 30):\n${items.slice(0, 30).map((item) => `- [${item.type}] ${item.title} | status=${item.status} | start=${item.startTime ? formatBangkokDateTime(item.startTime) : "unscheduled"} | end=${item.endTime ? formatBangkokDateTime(item.endTime) : "unscheduled"}`).join("\n")}` : "No schedule items found in the next 14 days."; }
